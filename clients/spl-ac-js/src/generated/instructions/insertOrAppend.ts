/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bytes,
  mapSerializer,
  struct,
  u32,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InsertOrAppendInstructionAccounts = {
  merkleTree: PublicKey | Pda;
  /**
   * Authority that controls write-access to the tree
   * Typically a program, e.g., the Bubblegum contract validates that leaves are valid NFTs.
   */

  authority?: Signer;
  /** Program used to emit changelogs as cpi instruction data. */
  noop: PublicKey | Pda;
};

// Data.
export type InsertOrAppendInstructionData = {
  discriminator: Array<number>;
  root: Uint8Array;
  leaf: Uint8Array;
  index: number;
};

export type InsertOrAppendInstructionDataArgs = {
  root: Uint8Array;
  leaf: Uint8Array;
  index: number;
};

export function getInsertOrAppendInstructionDataSerializer(): Serializer<
  InsertOrAppendInstructionDataArgs,
  InsertOrAppendInstructionData
> {
  return mapSerializer<
    InsertOrAppendInstructionDataArgs,
    any,
    InsertOrAppendInstructionData
  >(
    struct<InsertOrAppendInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['root', bytes({ size: 32 })],
        ['leaf', bytes({ size: 32 })],
        ['index', u32()],
      ],
      { description: 'InsertOrAppendInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [6, 42, 50, 190, 51, 109, 178, 168],
    })
  ) as Serializer<
    InsertOrAppendInstructionDataArgs,
    InsertOrAppendInstructionData
  >;
}

// Args.
export type InsertOrAppendInstructionArgs = InsertOrAppendInstructionDataArgs;

// Instruction.
export function insertOrAppend(
  context: Pick<Context, 'identity' | 'programs'>,
  input: InsertOrAppendInstructionAccounts & InsertOrAppendInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'splAccountCompression',
    'cmtDvXumGCrqC1Age74AVPhSRVXJMd8PJS91L8KbNCK'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {
    merkleTree: { index: 0, isWritable: true, value: input.merkleTree ?? null },
    authority: { index: 1, isWritable: false, value: input.authority ?? null },
    noop: { index: 2, isWritable: false, value: input.noop ?? null },
  };

  // Arguments.
  const resolvedArgs: InsertOrAppendInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInsertOrAppendInstructionDataSerializer().serialize(
    resolvedArgs as InsertOrAppendInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
