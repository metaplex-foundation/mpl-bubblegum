/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CloseEmptyTreeInstructionAccounts = {
  merkleTree: PublicKey | Pda;
  /** Authority that controls write-access to the tree */
  authority?: Signer;
  recipient: PublicKey | Pda;
};

// Data.
export type CloseEmptyTreeInstructionData = { discriminator: Array<number> };

export type CloseEmptyTreeInstructionDataArgs = {};

export function getCloseEmptyTreeInstructionDataSerializer(): Serializer<
  CloseEmptyTreeInstructionDataArgs,
  CloseEmptyTreeInstructionData
> {
  return mapSerializer<
    CloseEmptyTreeInstructionDataArgs,
    any,
    CloseEmptyTreeInstructionData
  >(
    struct<CloseEmptyTreeInstructionData>(
      [['discriminator', array(u8(), { size: 8 })]],
      { description: 'CloseEmptyTreeInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [50, 14, 219, 107, 78, 103, 16, 103],
    })
  ) as Serializer<
    CloseEmptyTreeInstructionDataArgs,
    CloseEmptyTreeInstructionData
  >;
}

// Instruction.
export function closeEmptyTree(
  context: Pick<Context, 'identity' | 'programs'>,
  input: CloseEmptyTreeInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'splAccountCompression',
    'cmtDvXumGCrqC1Age74AVPhSRVXJMd8PJS91L8KbNCK'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {
    merkleTree: { index: 0, isWritable: true, value: input.merkleTree ?? null },
    authority: { index: 1, isWritable: false, value: input.authority ?? null },
    recipient: { index: 2, isWritable: true, value: input.recipient ?? null },
  };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCloseEmptyTreeInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
