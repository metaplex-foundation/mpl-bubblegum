/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  struct,
  u32,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Data.
export type NoopInstructionInstructionData = { data: Uint8Array };

export type NoopInstructionInstructionDataArgs = NoopInstructionInstructionData;

export function getNoopInstructionInstructionDataSerializer(): Serializer<
  NoopInstructionInstructionDataArgs,
  NoopInstructionInstructionData
> {
  return struct<NoopInstructionInstructionData>(
    [['data', bytes({ size: u32() })]],
    { description: 'NoopInstructionInstructionData' }
  ) as Serializer<
    NoopInstructionInstructionDataArgs,
    NoopInstructionInstructionData
  >;
}

// Args.
export type NoopInstructionInstructionArgs = NoopInstructionInstructionDataArgs;

// Instruction.
export function noopInstruction(
  context: Pick<Context, 'programs'>,
  input: NoopInstructionInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'splNoop',
    'noopb9bkMVfRPU8AsbpTUg8AQkHtKwMYZiFUjNRtMmV'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {};

  // Arguments.
  const resolvedArgs: NoopInstructionInstructionArgs = { ...input };

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getNoopInstructionInstructionDataSerializer().serialize(
    resolvedArgs as NoopInstructionInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
