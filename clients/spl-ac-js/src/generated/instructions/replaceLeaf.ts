/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bytes,
  mapSerializer,
  struct,
  u32,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type ReplaceLeafInstructionAccounts = {
  merkleTree: PublicKey | Pda;
  /**
   * Authority that controls write-access to the tree
   * Typically a program, e.g., the Bubblegum contract validates that leaves are valid NFTs.
   */

  authority?: Signer;
  /** Program used to emit changelogs as cpi instruction data. */
  noop: PublicKey | Pda;
};

// Data.
export type ReplaceLeafInstructionData = {
  discriminator: Array<number>;
  root: Uint8Array;
  previousLeaf: Uint8Array;
  newLeaf: Uint8Array;
  index: number;
};

export type ReplaceLeafInstructionDataArgs = {
  root: Uint8Array;
  previousLeaf: Uint8Array;
  newLeaf: Uint8Array;
  index: number;
};

export function getReplaceLeafInstructionDataSerializer(): Serializer<
  ReplaceLeafInstructionDataArgs,
  ReplaceLeafInstructionData
> {
  return mapSerializer<
    ReplaceLeafInstructionDataArgs,
    any,
    ReplaceLeafInstructionData
  >(
    struct<ReplaceLeafInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['root', bytes({ size: 32 })],
        ['previousLeaf', bytes({ size: 32 })],
        ['newLeaf', bytes({ size: 32 })],
        ['index', u32()],
      ],
      { description: 'ReplaceLeafInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [204, 165, 76, 100, 73, 147, 0, 128],
    })
  ) as Serializer<ReplaceLeafInstructionDataArgs, ReplaceLeafInstructionData>;
}

// Args.
export type ReplaceLeafInstructionArgs = ReplaceLeafInstructionDataArgs;

// Instruction.
export function replaceLeaf(
  context: Pick<Context, 'identity' | 'programs'>,
  input: ReplaceLeafInstructionAccounts & ReplaceLeafInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'splAccountCompression',
    'cmtDvXumGCrqC1Age74AVPhSRVXJMd8PJS91L8KbNCK'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {
    merkleTree: { index: 0, isWritable: true, value: input.merkleTree ?? null },
    authority: { index: 1, isWritable: false, value: input.authority ?? null },
    noop: { index: 2, isWritable: false, value: input.noop ?? null },
  };

  // Arguments.
  const resolvedArgs: ReplaceLeafInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getReplaceLeafInstructionDataSerializer().serialize(
    resolvedArgs as ReplaceLeafInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
