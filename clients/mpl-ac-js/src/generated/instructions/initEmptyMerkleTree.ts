/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u32,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitEmptyMerkleTreeInstructionAccounts = {
  merkleTree: PublicKey | Pda;
  /**
   * Authority that controls write-access to the tree
   * Typically a program, e.g., the Bubblegum contract validates that leaves are valid NFTs.
   */

  authority?: Signer;
  /** Program used to emit changelogs as cpi instruction data. */
  noop: PublicKey | Pda;
};

// Data.
export type InitEmptyMerkleTreeInstructionData = {
  discriminator: Array<number>;
  maxDepth: number;
  maxBufferSize: number;
};

export type InitEmptyMerkleTreeInstructionDataArgs = {
  maxDepth: number;
  maxBufferSize: number;
};

export function getInitEmptyMerkleTreeInstructionDataSerializer(): Serializer<
  InitEmptyMerkleTreeInstructionDataArgs,
  InitEmptyMerkleTreeInstructionData
> {
  return mapSerializer<
    InitEmptyMerkleTreeInstructionDataArgs,
    any,
    InitEmptyMerkleTreeInstructionData
  >(
    struct<InitEmptyMerkleTreeInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['maxDepth', u32()],
        ['maxBufferSize', u32()],
      ],
      { description: 'InitEmptyMerkleTreeInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [191, 11, 119, 7, 180, 107, 220, 110],
    })
  ) as Serializer<
    InitEmptyMerkleTreeInstructionDataArgs,
    InitEmptyMerkleTreeInstructionData
  >;
}

// Args.
export type InitEmptyMerkleTreeInstructionArgs =
  InitEmptyMerkleTreeInstructionDataArgs;

// Instruction.
export function initEmptyMerkleTree(
  context: Pick<Context, 'identity' | 'programs'>,
  input: InitEmptyMerkleTreeInstructionAccounts &
    InitEmptyMerkleTreeInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplAccountCompression',
    'mcmt6YrQEMKw8Mw43FmpRLmf7BqRnFMKmAcbxE3xkAW'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {
    merkleTree: { index: 0, isWritable: true, value: input.merkleTree ?? null },
    authority: { index: 1, isWritable: false, value: input.authority ?? null },
    noop: { index: 2, isWritable: false, value: input.noop ?? null },
  };

  // Arguments.
  const resolvedArgs: InitEmptyMerkleTreeInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitEmptyMerkleTreeInstructionDataSerializer().serialize(
    resolvedArgs as InitEmptyMerkleTreeInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
