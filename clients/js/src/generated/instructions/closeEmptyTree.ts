/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  AccountMeta,
  Context,
  PublicKey,
  Serializer,
  Signer,
  TransactionBuilder,
  mapSerializer,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import { addObjectProperty, isWritable } from '../shared';

// Accounts.
export type CloseEmptyTreeInstructionAccounts = {
  merkleTree: PublicKey;
  authority?: Signer;
  recipient: PublicKey;
};

// Data.
export type CloseEmptyTreeInstructionData = { discriminator: Array<number> };

export type CloseEmptyTreeInstructionDataArgs = {};

export function getCloseEmptyTreeInstructionDataSerializer(
  context: Pick<Context, 'serializer'>
): Serializer<
  CloseEmptyTreeInstructionDataArgs,
  CloseEmptyTreeInstructionData
> {
  const s = context.serializer;
  return mapSerializer<
    CloseEmptyTreeInstructionDataArgs,
    any,
    CloseEmptyTreeInstructionData
  >(
    s.struct<CloseEmptyTreeInstructionData>(
      [['discriminator', s.array(s.u8(), { size: 8 })]],
      { description: 'CloseEmptyTreeInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [50, 14, 219, 107, 78, 103, 16, 103],
    })
  ) as Serializer<
    CloseEmptyTreeInstructionDataArgs,
    CloseEmptyTreeInstructionData
  >;
}

// Instruction.
export function closeEmptyTree(
  context: Pick<Context, 'serializer' | 'programs' | 'identity'>,
  input: CloseEmptyTreeInstructionAccounts
): TransactionBuilder {
  const signers: Signer[] = [];
  const keys: AccountMeta[] = [];

  // Program ID.
  const programId = {
    ...context.programs.getPublicKey(
      'splAccountCompression',
      'cmtDvXumGCrqC1Age74AVPhSRVXJMd8PJS91L8KbNCK'
    ),
    isWritable: false,
  };

  // Resolved inputs.
  const resolvingAccounts = {};
  addObjectProperty(
    resolvingAccounts,
    'authority',
    input.authority ?? context.identity
  );
  const resolvedAccounts = { ...input, ...resolvingAccounts };

  // Merkle Tree.
  keys.push({
    pubkey: resolvedAccounts.merkleTree,
    isSigner: false,
    isWritable: isWritable(resolvedAccounts.merkleTree, true),
  });

  // Authority.
  signers.push(resolvedAccounts.authority);
  keys.push({
    pubkey: resolvedAccounts.authority.publicKey,
    isSigner: true,
    isWritable: isWritable(resolvedAccounts.authority, false),
  });

  // Recipient.
  keys.push({
    pubkey: resolvedAccounts.recipient,
    isSigner: false,
    isWritable: isWritable(resolvedAccounts.recipient, true),
  });

  // Data.
  const data = getCloseEmptyTreeInstructionDataSerializer(context).serialize(
    {}
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
