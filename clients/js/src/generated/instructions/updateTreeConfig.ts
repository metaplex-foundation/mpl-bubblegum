/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  mapSerializer,
  option,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findTreeConfigPda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';
import {
  DecompressibleState,
  DecompressibleStateArgs,
  getDecompressibleStateSerializer,
} from '../types';

// Accounts.
export type UpdateTreeConfigInstructionAccounts = {
  treeConfig?: PublicKey | Pda;
  authority?: Signer;
  merkleTree: PublicKey | Pda;
};

// Data.
export type UpdateTreeConfigInstructionData = {
  discriminator: Array<number>;
  treeCreator: Option<PublicKey>;
  treeDelegate: Option<PublicKey>;
  isPublic: Option<boolean>;
  isDecompressible: Option<DecompressibleState>;
};

export type UpdateTreeConfigInstructionDataArgs = {
  treeCreator: OptionOrNullable<PublicKey>;
  treeDelegate: OptionOrNullable<PublicKey>;
  isPublic: OptionOrNullable<boolean>;
  isDecompressible: OptionOrNullable<DecompressibleStateArgs>;
};

export function getUpdateTreeConfigInstructionDataSerializer(): Serializer<
  UpdateTreeConfigInstructionDataArgs,
  UpdateTreeConfigInstructionData
> {
  return mapSerializer<
    UpdateTreeConfigInstructionDataArgs,
    any,
    UpdateTreeConfigInstructionData
  >(
    struct<UpdateTreeConfigInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['treeCreator', option(publicKeySerializer())],
        ['treeDelegate', option(publicKeySerializer())],
        ['isPublic', option(bool())],
        ['isDecompressible', option(getDecompressibleStateSerializer())],
      ],
      { description: 'UpdateTreeConfigInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [75, 1, 47, 108, 102, 26, 239, 207],
    })
  ) as Serializer<
    UpdateTreeConfigInstructionDataArgs,
    UpdateTreeConfigInstructionData
  >;
}

// Args.
export type UpdateTreeConfigInstructionArgs =
  UpdateTreeConfigInstructionDataArgs;

// Instruction.
export function updateTreeConfig(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: UpdateTreeConfigInstructionAccounts & UpdateTreeConfigInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplBubblegum',
    'BGUMAp9Gq7iTEuizy4pqaxsTyUCBK68MDfK752saRPUY'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {
    treeConfig: { index: 0, isWritable: true, value: input.treeConfig ?? null },
    authority: { index: 1, isWritable: false, value: input.authority ?? null },
    merkleTree: { index: 2, isWritable: true, value: input.merkleTree ?? null },
  };

  // Arguments.
  const resolvedArgs: UpdateTreeConfigInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.treeConfig.value) {
    resolvedAccounts.treeConfig.value = findTreeConfigPda(context, {
      merkleTree: expectPublicKey(resolvedAccounts.merkleTree.value),
    });
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateTreeConfigInstructionDataSerializer().serialize(
    resolvedArgs as UpdateTreeConfigInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
