/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  AccountMeta,
  Context,
  PublicKey,
  Serializer,
  Signer,
  TransactionBuilder,
  mapSerializer,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import { addObjectProperty, isWritable } from '../shared';

// Accounts.
export type InitEmptyMerkleTreeInstructionAccounts = {
  merkleTree: PublicKey;
  authority?: Signer;
  noop: PublicKey;
};

// Data.
export type InitEmptyMerkleTreeInstructionData = {
  discriminator: Array<number>;
  maxDepth: number;
  maxBufferSize: number;
};

export type InitEmptyMerkleTreeInstructionDataArgs = {
  maxDepth: number;
  maxBufferSize: number;
};

export function getInitEmptyMerkleTreeInstructionDataSerializer(
  context: Pick<Context, 'serializer'>
): Serializer<
  InitEmptyMerkleTreeInstructionDataArgs,
  InitEmptyMerkleTreeInstructionData
> {
  const s = context.serializer;
  return mapSerializer<
    InitEmptyMerkleTreeInstructionDataArgs,
    any,
    InitEmptyMerkleTreeInstructionData
  >(
    s.struct<InitEmptyMerkleTreeInstructionData>(
      [
        ['discriminator', s.array(s.u8(), { size: 8 })],
        ['maxDepth', s.u32()],
        ['maxBufferSize', s.u32()],
      ],
      { description: 'InitEmptyMerkleTreeInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [191, 11, 119, 7, 180, 107, 220, 110],
    })
  ) as Serializer<
    InitEmptyMerkleTreeInstructionDataArgs,
    InitEmptyMerkleTreeInstructionData
  >;
}

// Args.
export type InitEmptyMerkleTreeInstructionArgs =
  InitEmptyMerkleTreeInstructionDataArgs;

// Instruction.
export function initEmptyMerkleTree(
  context: Pick<Context, 'serializer' | 'programs' | 'identity'>,
  input: InitEmptyMerkleTreeInstructionAccounts &
    InitEmptyMerkleTreeInstructionArgs
): TransactionBuilder {
  const signers: Signer[] = [];
  const keys: AccountMeta[] = [];

  // Program ID.
  const programId = {
    ...context.programs.getPublicKey(
      'splAccountCompression',
      'cmtDvXumGCrqC1Age74AVPhSRVXJMd8PJS91L8KbNCK'
    ),
    isWritable: false,
  };

  // Resolved inputs.
  const resolvingAccounts = {};
  const resolvingArgs = {};
  addObjectProperty(
    resolvingAccounts,
    'authority',
    input.authority ?? context.identity
  );
  const resolvedAccounts = { ...input, ...resolvingAccounts };
  const resolvedArgs = { ...input, ...resolvingArgs };

  // Merkle Tree.
  keys.push({
    pubkey: resolvedAccounts.merkleTree,
    isSigner: false,
    isWritable: isWritable(resolvedAccounts.merkleTree, true),
  });

  // Authority.
  signers.push(resolvedAccounts.authority);
  keys.push({
    pubkey: resolvedAccounts.authority.publicKey,
    isSigner: true,
    isWritable: isWritable(resolvedAccounts.authority, false),
  });

  // Noop.
  keys.push({
    pubkey: resolvedAccounts.noop,
    isSigner: false,
    isWritable: isWritable(resolvedAccounts.noop, false),
  });

  // Data.
  const data =
    getInitEmptyMerkleTreeInstructionDataSerializer(context).serialize(
      resolvedArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
