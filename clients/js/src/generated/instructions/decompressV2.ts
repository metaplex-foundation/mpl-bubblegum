/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  none,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bytes,
  mapSerializer,
  option,
  struct,
  u32,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findTreeConfigPda } from '../accounts';
import {
  PickPartial,
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';
import {
  MetadataArgsV2,
  MetadataArgsV2Args,
  getMetadataArgsV2Serializer,
} from '../types';

// Accounts.
export type DecompressV2InstructionAccounts = {
  treeConfig?: PublicKey | Pda;
  payer?: Signer;
  /**
   * Optional leaf authority, defaults to `payer`. Must be either the leaf
   * owner or the leaf delegate.
   */

  leafAuthority?: Signer;
  /** Becomes the owner of the new MPL Core asset. */
  leafOwner: PublicKey | Pda;
  /** Defaults to `leafOwner`. */
  leafDelegate?: PublicKey | Pda;
  merkleTree: PublicKey | Pda;
  /** Fresh signer for the new MPL Core asset account. */
  newAsset: Signer;
  /** The MPL Core collection the leaf belonged to. Required. */
  coreCollection: PublicKey | Pda;
  mplCoreCpiSigner?: PublicKey | Pda;
  logWrapper?: PublicKey | Pda;
  compressionProgram?: PublicKey | Pda;
  mplCoreProgram?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type DecompressV2InstructionData = {
  discriminator: Array<number>;
  root: Uint8Array;
  nonce: bigint;
  index: number;
  metadata: MetadataArgsV2;
  assetDataHash: Option<Uint8Array>;
  flags: Option<number>;
};

export type DecompressV2InstructionDataArgs = {
  root: Uint8Array;
  nonce: number | bigint;
  index: number;
  metadata: MetadataArgsV2Args;
  assetDataHash?: OptionOrNullable<Uint8Array>;
  flags?: OptionOrNullable<number>;
};

export function getDecompressV2InstructionDataSerializer(): Serializer<
  DecompressV2InstructionDataArgs,
  DecompressV2InstructionData
> {
  return mapSerializer<
    DecompressV2InstructionDataArgs,
    any,
    DecompressV2InstructionData
  >(
    struct<DecompressV2InstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['root', bytes({ size: 32 })],
        ['nonce', u64()],
        ['index', u32()],
        ['metadata', getMetadataArgsV2Serializer()],
        ['assetDataHash', option(bytes({ size: 32 }))],
        ['flags', option(u8())],
      ],
      { description: 'DecompressV2InstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [222, 204, 18, 191, 82, 195, 125, 153],
      assetDataHash: value.assetDataHash ?? none(),
      flags: value.flags ?? none(),
    })
  ) as Serializer<DecompressV2InstructionDataArgs, DecompressV2InstructionData>;
}

// Extra Args.
export type DecompressV2InstructionExtraArgs = { proof?: Array<PublicKey> };

// Args.
export type DecompressV2InstructionArgs = PickPartial<
  DecompressV2InstructionDataArgs & DecompressV2InstructionExtraArgs,
  'proof'
>;

// Instruction.
export function decompressV2(
  context: Pick<Context, 'eddsa' | 'payer' | 'programs'>,
  input: DecompressV2InstructionAccounts & DecompressV2InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplBubblegum',
    'BGUMAp9Gq7iTEuizy4pqaxsTyUCBK68MDfK752saRPUY'
  );

  // Accounts.
  const resolvedAccounts = {
    treeConfig: {
      index: 0,
      isWritable: true as boolean,
      value: input.treeConfig ?? null,
    },
    payer: {
      index: 1,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    leafAuthority: {
      index: 2,
      isWritable: false as boolean,
      value: input.leafAuthority ?? null,
    },
    leafOwner: {
      index: 3,
      isWritable: false as boolean,
      value: input.leafOwner ?? null,
    },
    leafDelegate: {
      index: 4,
      isWritable: false as boolean,
      value: input.leafDelegate ?? null,
    },
    merkleTree: {
      index: 5,
      isWritable: true as boolean,
      value: input.merkleTree ?? null,
    },
    newAsset: {
      index: 6,
      isWritable: true as boolean,
      value: input.newAsset ?? null,
    },
    coreCollection: {
      index: 7,
      isWritable: true as boolean,
      value: input.coreCollection ?? null,
    },
    mplCoreCpiSigner: {
      index: 8,
      isWritable: false as boolean,
      value: input.mplCoreCpiSigner ?? null,
    },
    logWrapper: {
      index: 9,
      isWritable: false as boolean,
      value: input.logWrapper ?? null,
    },
    compressionProgram: {
      index: 10,
      isWritable: false as boolean,
      value: input.compressionProgram ?? null,
    },
    mplCoreProgram: {
      index: 11,
      isWritable: false as boolean,
      value: input.mplCoreProgram ?? null,
    },
    systemProgram: {
      index: 12,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: DecompressV2InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.treeConfig.value) {
    resolvedAccounts.treeConfig.value = findTreeConfigPda(context, {
      merkleTree: expectPublicKey(resolvedAccounts.merkleTree.value),
    });
  }
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.mplCoreCpiSigner.value) {
    resolvedAccounts.mplCoreCpiSigner.value = publicKey(
      'CbNY3JiXdXNE9tPNEk1aRZVEkWdj2v7kfJLNQwZZgpXk'
    );
  }
  if (!resolvedAccounts.logWrapper.value) {
    resolvedAccounts.logWrapper.value = context.programs.getPublicKey(
      'mplNoop',
      'mnoopTCrg4p8ry25e4bcWA9XZjbNjMTfgYVGGEdRsf3'
    );
    resolvedAccounts.logWrapper.isWritable = false;
  }
  if (!resolvedAccounts.compressionProgram.value) {
    resolvedAccounts.compressionProgram.value = context.programs.getPublicKey(
      'mplAccountCompression',
      'mcmt6YrQEMKw8Mw43FmpRLmf7BqRnFMKmAcbxE3xkAW'
    );
    resolvedAccounts.compressionProgram.isWritable = false;
  }
  if (!resolvedAccounts.mplCoreProgram.value) {
    resolvedAccounts.mplCoreProgram.value = context.programs.getPublicKey(
      'mplCore',
      'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
    );
    resolvedAccounts.mplCoreProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  if (!resolvedArgs.proof) {
    resolvedArgs.proof = [];
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Remaining Accounts.
  const remainingAccounts = resolvedArgs.proof.map((value, index) => ({
    index,
    value,
    isWritable: false,
  }));
  orderedAccounts.push(...remainingAccounts);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getDecompressV2InstructionDataSerializer().serialize(
    resolvedArgs as DecompressV2InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
